# 前端开发指南

> BladeX 框架下的前端代码开发规范

---

## 一、目录结构

```
ed-station-web/src/
├── api/                # API 接口
│   └── desk/
│       └── xxx.js
└── views/              # 页面组件
    └── desk/
        └── xxx.vue
```

---

## 二、API 接口封装

**文件**: `src/api/desk/xxx.js`

```javascript
import request from '@/axios';

// 分页查询
export const getList = (current, size, params) => {
  return request({
    url: '/blade-desk/innermessage/list',
    method: 'get',
    params: {
      ...params,
      current,
      size,
    },
    cryptoToken: false,
    cryptoData: false,
  });
};

// 删除
export const remove = ids => {
  return request({
    url: '/blade-desk/innermessage/remove',
    method: 'post',
    params: {
      ids,
    },
    cryptoToken: false,
    cryptoData: false,
  });
};

// 新增
export const add = row => {
  return request({
    url: '/blade-desk/innermessage/submit',
    method: 'post',
    data: row,
    cryptoToken: false,
    cryptoData: false,
  });
};

// 修改
export const update = row => {
  return request({
    url: '/blade-desk/innermessage/submit',
    method: 'post',
    data: row,
    cryptoToken: false,
    cryptoData: false,
  });
};

// 详情
export const getDetail = id => {
  return request({
    url: '/blade-desk/innermessage/detail',
    method: 'get',
    params: {
      id,
    },
    cryptoToken: false,
    cryptoData: false,
  });
};
```

### 要点

- ✅ 使用 `cryptoToken: false` 和 `cryptoData: false` 禁用加密（根据项目需求）
- ✅ 新增和修改可共用 `/submit` 接口
- ✅ URL 与后端接口保持一致
- ✅ 统一使用 `request` 封装的 axios 实例

---

## 三、页面组件（Vue）

**文件**: `src/views/desk/xxx.vue`

### 3.1 模板结构

```vue
<template>
  <basic-container>
    <avue-crud
      :option="option"
      :table-loading="loading"
      :data="data"
      v-model:page="page"
      ref="crud"
      @row-del="rowDel"
      v-model="form"
      :permission="permissionList"
      @row-update="rowUpdate"
      @row-save="rowSave"
      :before-open="beforeOpen"
      @search-change="searchChange"
      @search-reset="searchReset"
      @selection-change="selectionChange"
      @current-change="currentChange"
      @size-change="sizeChange"
      @refresh-change="refreshChange"
      @on-load="onLoad"
    >
      <!-- 自定义按钮 -->
      <template #menu-left>
        <el-button
          type="danger"
          icon="el-icon-delete"
          plain
          v-if="permission.innermessage_delete"
          @click="handleDelete"
          >批量删除
        </el-button>
      </template>
      
      <!-- 自定义列 -->
      <template #messageType="{ row }">
        <el-tag>{{ row.messageTypeName }}</el-tag>
      </template>
    </avue-crud>
  </basic-container>
</template>
```

### 3.2 脚本结构

```vue
<script>
import { getList, remove, update, add, getDetail } from '@/api/desk/innermessage';
import { mapGetters } from 'vuex';

export default {
  data() {
    return {
      form: {},
      query: {},
      loading: true,
      page: {
        pageSize: 10,
        currentPage: 1,
        total: 0,
      },
      selectionList: [],
      option: {
        // Avue 配置
      },
      data: [],
    };
  },
  computed: {
    ...mapGetters(['permission']),
    permissionList() {
      return {
        addBtn: this.validData(this.permission.innermessage_add, false),
        viewBtn: this.validData(this.permission.innermessage_view, false),
        delBtn: this.validData(this.permission.innermessage_delete, false),
        editBtn: this.validData(this.permission.innermessage_edit, false),
      };
    },
    ids() {
      let ids = [];
      this.selectionList.forEach(ele => {
        ids.push(ele.id);
      });
      return ids.join(',');
    },
  },
  methods: {
    // 新增
    rowSave(row, done, loading) {
      add(row).then(
        () => {
          this.onLoad(this.page);
          this.$message.success('操作成功!');
          done();
        },
        error => {
          window.console.log(error);
          loading();
        }
      );
    },
    // 修改
    rowUpdate(row, index, done, loading) {
      update(row).then(
        () => {
          this.onLoad(this.page);
          this.$message.success('操作成功!');
          done();
        },
        error => {
          window.console.log(error);
          loading();
        }
      );
    },
    // 删除
    rowDel(row) {
      this.$confirm('确定将选择数据删除?', {
        confirmButtonText: '确定',
        cancelButtonText: '取消',
        type: 'warning',
      })
        .then(() => {
          return remove(row.id);
        })
        .then(() => {
          this.onLoad(this.page);
          this.$message.success('操作成功!');
        });
    },
    // 搜索重置
    searchReset() {
      this.query = {};
      this.onLoad(this.page);
    },
    // 搜索
    searchChange(params, done) {
      this.query = params;
      this.page.currentPage = 1;
      this.onLoad(this.page, params);
      done();
    },
    // 选择变化
    selectionChange(list) {
      this.selectionList = list;
    },
    // 清空选择
    selectionClear() {
      this.selectionList = [];
      this.$refs.crud.toggleSelection();
    },
    // 批量删除
    handleDelete() {
      if (this.selectionList.length === 0) {
        this.$message.warning('请选择至少一条数据');
        return;
      }
      this.$confirm('确定将选择数据删除?', {
        confirmButtonText: '确定',
        cancelButtonText: '取消',
        type: 'warning',
      })
        .then(() => {
          return remove(this.ids);
        })
        .then(() => {
          this.onLoad(this.page);
          this.$message.success('操作成功!');
          this.$refs.crud.toggleSelection();
        });
    },
    // 打开弹窗前
    beforeOpen(done, type) {
      if (['edit', 'view'].includes(type)) {
        getDetail(this.form.id).then(res => {
          this.form = res.data.data;
        });
      }
      done();
    },
    // 分页变化
    currentChange(currentPage) {
      this.page.currentPage = currentPage;
    },
    sizeChange(pageSize) {
      this.page.pageSize = pageSize;
    },
    refreshChange() {
      this.onLoad(this.page, this.query);
    },
    // 加载数据
    onLoad(page, params = {}) {
      const { releaseTimeRange } = this.query;
      let values = {
        ...params,
        ...this.query,
      };
      // 处理日期范围
      if (releaseTimeRange) {
        values = {
          ...values,
          releaseTime_datege: releaseTimeRange[0],
          releaseTime_datelt: releaseTimeRange[1],
        };
        values.releaseTimeRange = null;
      }
      this.loading = true;
      getList(page.currentPage, page.pageSize, values).then(res => {
        const data = res.data.data;
        this.page.total = data.total;
        this.data = data.records;
        this.loading = false;
        this.selectionClear();
      });
    },
  },
};
</script>
```

---

## 四、Avue 配置要点

### 4.1 基础配置

```javascript
option: {
  height: 'auto',           // 表格高度
  calcHeight: 32,           // 高度计算
  dialogWidth: 950,         // 弹窗宽度
  searchShow: true,         // 显示搜索栏
  searchMenuSpan: 6,        // 搜索菜单跨度
  border: true,             // 边框
  index: true,              // 序号列
  viewBtn: true,            // 查看按钮
  selection: true,          // 多选列
  excelBtn: true,           // 导出按钮
  dialogClickModal: false,  // 点击弹窗外部不关闭
  grid: false,              // 栅格布局
}
```

### 4.2 列配置

```javascript
column: [
  // 普通文本字段
  {
    label: '标题',
    prop: 'title',
    span: 24,               // 字段跨度
    row: true,              // 独占一行
    search: true,           // 可搜索
    rules: [
      {
        required: true,
        message: '请输入标题',
        trigger: 'blur',
      },
    ],
  },
  
  // 字典下拉选择
  {
    label: '消息类型',
    type: 'select',
    dicUrl: '/blade-system/dict/dictionary?code=message_type',
    props: {
      label: 'dictValue',
      value: 'dictKey',
    },
    dataType: 'number',
    slot: true,
    prop: 'messageType',
    search: true,
    rules: [
      {
        required: true,
        message: '请输入消息类型',
        trigger: 'blur',
      },
    ],
  },
  
  // 日期时间范围
  {
    label: '发布时间',
    prop: 'releaseTimeRange',
    type: 'datetime',
    format: 'YYYY-MM-DD HH:mm:ss',
    valueFormat: 'YYYY-MM-DD HH:mm:ss',
    searchRange: true,      // 范围搜索
    hide: true,             // 不在表格显示
    addDisplay: false,      // 新增时隐藏
    editDisplay: false,     // 编辑时隐藏
    viewDisplay: false,     // 查看时隐藏
    search: true,
  },
  
  // 单个日期字段
  {
    label: '发布日期',
    prop: 'releaseTime',
    type: 'date',
    gridRow: true,
    format: 'YYYY-MM-DD HH:mm:ss',
    valueFormat: 'YYYY-MM-DD HH:mm:ss',
    rules: [
      {
        required: true,
        message: '请输入发布日期',
        trigger: 'click',
      },
    ],
  },
  
  // 富文本编辑器
  {
    label: '消息内容',
    prop: 'messageContent',
    component: 'avue-ueditor',
    action: '/blade-resource/oss/endpoint/put-file',
    propsHttp: {
      res: 'data',
      url: 'link',
    },
    hide: true,
    minRows: 4,
    span: 24,
  },
]
```

### 4.3 自定义列显示

使用 slot 自定义列显示（如字典名称）：

```vue
<template #messageType="{ row }">
  <el-tag>{{ row.messageTypeName }}</el-tag>
</template>
```

---

## 五、权限控制配置

```javascript
computed: {
  ...mapGetters(['permission']),
  permissionList() {
    return {
      addBtn: this.validData(this.permission.innermessage_add, false),
      viewBtn: this.validData(this.permission.innermessage_view, false),
      delBtn: this.validData(this.permission.innermessage_delete, false),
      editBtn: this.validData(this.permission.innermessage_edit, false),
    };
  },
}
```

---

## 六、日期范围查询处理

```javascript
// 在 onLoad 方法中处理
onLoad(page, params = {}) {
  const { releaseTimeRange } = this.query;
  let values = { ...params, ...this.query };
  
  if (releaseTimeRange) {
    values = {
      ...values,
      releaseTime_datege: releaseTimeRange[0],  // 大于等于
      releaseTime_datelt: releaseTimeRange[1],  // 小于等于
    };
    values.releaseTimeRange = null;
  }
  
  getList(page.currentPage, page.pageSize, values).then(...);
}
```

---

## 七、检查清单

### 开发前检查
- [ ] API 接口文件已创建
- [ ] Vue 页面组件已创建
- [ ] 路由配置已添加

### 代码检查
- [ ] Avue 配置完整
- [ ] 字典接口 URL 正确
- [ ] 权限码与后端一致
- [ ] 日期范围查询处理正确
- [ ] 表单验证规则完整

### 功能测试检查
- [ ] 列表显示正常
- [ ] 分页功能正常
- [ ] 搜索功能正常
- [ ] 新增功能正常
- [ ] 修改功能正常
- [ ] 删除功能正常（单条 + 批量）
- [ ] 字典下拉正常
- [ ] 权限按钮控制正常

---

## 🔗 相关文档

- [开发流程总览](./01-开发流程总览.md) - 完整开发流程
- [接口公约规范](./02-接口公约规范.md) - 前后端接口规范
- [后端开发指南](./05-后端开发指南.md) - Controller 开发
