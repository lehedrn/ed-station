# 阶段 3A：后端开发提示词

> 用于后端实体、Mapper、Service、Controller 开发

---

## 使用说明

本提示词文档用于指导完成后端开发阶段的任务：
1. **Entity 和 VO 类** - 实体类和视图对象
2. **Mapper 层** - 数据访问层
3. **Service 层** - 业务逻辑层
4. **Controller 层** - 接口控制层
5. **单元测试** - 接口测试

---

## 提示词 3A-1：Entity 和 VO 类开发

**使用场景**：接口契约确认后，开始后端实体类开发

**复制以下内容给 Claude**：

```
我已经完成了阶段 2（接口定义），现在开始后端开发。

## 项目信息

- 模块名称：{模块名称}
- 模块代码：{模块代码}
- 所属模块目录：{所属模块目录}
- 后端项目路径：{后端项目路径}
- 数据库表名：{数据库表名}

## 已完成的工作

- [ ] 接口契约已确认
- [ ] Controller 框架已生成

## 任务要求

### 1. Entity 实体类

请根据数据库表结构，生成 Entity 实体类：

**要求**：
- 遵循 BladeX 规范
- 继承 BladeX 的 BaseEntity
- 使用 Lombok 注解
- 包含所有字段
- 字段注释完整

```java
package org.springblade.{模块目录}.entity;

import com.baomidou.mybatisplus.annotation.*;
import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import lombok.EqualsAndHashCode;
import org.springblade.core.mp.base.BaseEntity;

/**
 * {模块名称} 实体类
 *
 * @author {作者}
 * @since {日期}
 */
@Data
@EqualsAndHashCode(callSuper = true)
@TableName("blade_{模块代码}")
@Schema(description = "{模块名称}对象")
public class {模块类名} extends BaseEntity {

    private static final long serialVersionUID = 1L;

    /**
     * 主键 ID
     */
    @TableId(value = "id", type = IdType.ASSIGN_ID)
    @Schema(description = "主键 ID")
    private Long id;

    /**
     * 租户 ID
     */
    @Schema(description = "租户 ID")
    private String tenantId;

    // ... 其他业务字段

    /**
     * 创建人
     */
    @Schema(description = "创建人")
    private Long createUser;

    /**
     * 创建时间
     */
    @Schema(description = "创建时间")
    private LocalDateTime createTime;

    /**
     * 修改人
     */
    @Schema(description = "修改人")
    private Long updateUser;

    /**
     * 修改时间
     */
    @Schema(description = "修改时间")
    private LocalDateTime updateTime;

    /**
     * 状态
     */
    @Schema(description = "状态")
    private Integer status;

    /**
     * 是否已删除
     */
    @TableLogic
    @Schema(description = "是否已删除")
    private Integer isDeleted;
}
```

### 2. VO 视图对象

请生成前端展示用的 VO 类：

**要求**：
- 包含前端需要的所有字段
- 包含字典转换后的字段（如 affairTypeDict）
- 包含子表数据（如有）

```java
package org.springblade.{模块目录}.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import lombok.EqualsAndHashCode;
import java.util.List;

/**
 * {模块名称} 视图对象
 *
 * @author {作者}
 * @since {日期}
 */
@Data
@EqualsAndHashCode(callSuper = true)
public class {模块类名}VO extends {模块类名} {

    private static final long serialVersionUID = 1L;

    // 字典转换字段
    @Schema(description = "事项类别字典值")
    private String affairTypeDict;

    @Schema(description = "状态字典值")
    private String statusDict;

    // 子表数据（如有）
    @Schema(description = "材料列表")
    private List<{子表类名}VO> materials;
}
```

### 3. DTO 和 Wrapper（如需要）

请生成新增/修改用的 DTO 类：

```java
package org.springblade.{模块目录}.dto;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import lombok.EqualsAndHashCode;
import javax.validation.constraints.*;
import java.util.List;

/**
 * {模块名称} 保存 DTO
 *
 * @author {作者}
 * @since {日期}
 */
@Data
public class {模块类名}SaveVO {

    @Schema(description = "事项名称")
    @NotBlank(message = "事项名称不能为空")
    @Size(max = 200, message = "事项名称不能超过 200 字符")
    private String affairName;

    @Schema(description = "事项类别")
    @NotBlank(message = "事项类别不能为空")
    private String affairType;

    // ... 其他字段

    @Schema(description = "材料列表")
    private List<MaterialSaveVO> materials;
}
```

## 输出要求

1. 代码符合 BladeX 规范
2. 注释完整准确
3. 使用 Lombok 简化代码
4. 字段校验规则完整

## 补充说明

BladeX 实体类规范：
- 继承 BaseEntity（包含 create_user、create_time 等字段）
- 使用@TableName 指定表名
- 使用@TableId 指定主键策略
- 使用@TableLogic 指定逻辑删除字段
- 使用@Schema 添加 Swagger 文档注释
```

---

## 提示词 3A-2：Mapper 层开发

**使用场景**：Entity 类完成后，开发 Mapper 层

**复制以下内容给 Claude**：

```
Entity 和 VO 类已完成，现在需要开发 Mapper 层。

## 项目信息

- 模块名称：{模块名称}
- 模块代码：{模块代码}
- 后端项目路径：{后端项目路径}

## 任务要求

### 1. Mapper 接口

请生成 Mapper 接口：

```java
package org.springblade.{模块目录}.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.baomidou.mybatisplus.core.metadata.IPage;
import org.apache.ibatis.annotations.Param;
import org.springblade.{模块目录}.entity.{模块类名};
import org.springblade.{模块目录}.vo.{模块类名}VO;

/**
 * {模块名称} Mapper 接口
 *
 * @author {作者}
 * @since {日期}
 */
public interface {模块类名}Mapper extends BaseMapper<{模块类名}> {

    /**
     * 分页查询
     * @param page 分页对象
     * @param params 查询参数
     * @return 分页结果
     */
    IPage<{模块类名}VO> select{模块类名}Page(IPage page, @Param("params") Map<String, Object> params);

    /**
     * 详情查询
     * @param id 主键 ID
     * @return 详情对象
     */
    {模块类名}VO selectById(@Param("id") Long id);
}
```

### 2. Mapper XML

请生成 Mapper XML 文件：

**文件路径**：`src/main/resources/mapper/{模块目录}/{模块类名}/{模块类名}.xml`

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.springblade.{模块目录}.mapper.{模块类名}Mapper">

    <!-- 结果映射 -->
    <resultMap id="{模块类名}VOResultMap" type="org.springblade.{模块目录}.vo.{模块类名}VO">
        <id column="id" property="id"/>
        <result column="affair_name" property="affairName"/>
        <!-- ... 其他字段 ... -->
        <!-- 子表关联查询（如有） -->
        <collection property="materials" ofType="org.springblade.{模块目录}.vo.MaterialVO">
            <id column="material_id" property="id"/>
            <result column="material_name" property="materialName"/>
            <!-- ... 其他字段 ... -->
        </collection>
    </resultMap>

    <!-- 分页查询 -->
    <select id="select{模块类名}Page" resultMap="{模块类名}VOResultMap">
        SELECT * FROM blade_{模块代码}
        <where>
            is_deleted = 0
            <if test="params.affairName != null and params.affairName != ''">
                AND affair_name LIKE CONCAT('%', #{params.affairName}, '%')
            </if>
            <if test="params.affairType != null and params.affairType != ''">
                AND affair_type = #{params.affairType}
            </if>
            <if test="params.status != null">
                AND status = #{params.status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 详情查询 -->
    <select id="selectById" resultMap="{模块类名}VOResultMap">
        SELECT * FROM blade_{模块代码}
        WHERE id = #{id} AND is_deleted = 0
    </select>
</mapper>
```

## 输出要求

1. Mapper 接口遵循 BladeX 规范
2. XML 配置使用 ResultMap 处理关联查询
3. 查询条件使用动态 SQL
4. 包含逻辑删除条件

## 补充说明

MyBatis-Plus 规范：
- 继承 BaseMapper 获得基础 CRUD 能力
- 使用@Param 注解命名参数
- XML 文件路径与 Mapper 接口对应
- 使用动态 SQL 处理可选查询条件
```

---

## 提示词 3A-3：Service 层开发

**使用场景**：Mapper 层完成后，开发 Service 层

**复制以下内容给 Claude**：

```
Mapper 层已完成，现在需要开发 Service 层。

## 项目信息

- 模块名称：{模块名称}
- 模块代码：{模块代码}
- 后端项目路径：{后端项目路径}

## 任务要求

### 1. Service 接口

请生成 Service 接口：

```java
package org.springblade.{模块目录}.service;

import com.baomidou.mybatisplus.core.metadata.IPage;
import org.springblade.core.mp.base.BaseService;
import org.springblade.{模块目录}.entity.{模块类名};
import org.springblade.{模块目录}.dto.{模块类名}SaveVO;
import org.springblade.{模块目录}.dto.{模块类名}UpdateVO;
import org.springblade.{模块目录}.vo.{模块类名}VO;
import java.util.Map;

/**
 * {模块名称} Service 接口
 *
 * @author {作者}
 * @since {日期}
 */
public interface I{模块类名}Service extends BaseService<{模块类名}> {

    /**
     * 分页查询
     * @param page 分页对象
     * @param params 查询参数
     * @return 分页结果
     */
    IPage<{模块类名}VO> pageList(IPage page, Map<String, Object> params);

    /**
     * 详情查询
     * @param id 主键 ID
     * @return 详情对象
     */
    {模块类名}VO getDetail(Long id);

    /**
     * 新增
     * @param vo 保存 DTO
     * @return 是否成功
     */
    Boolean save{模块类名}({模块类名}SaveVO vo);

    /**
     * 修改
     * @param vo 修改 DTO
     * @return 是否成功
     */
    Boolean update{模块类名}({模块类名}UpdateVO vo);

    /**
     * 删除
     * @param ids 主键 ID 列表（逗号分隔）
     * @return 是否成功
     */
    Boolean remove{模块类名}(String ids);

    /**
     * 发布
     * @param id 主键 ID
     * @return 是否成功
     */
    Boolean publish(Long id);

    /**
     * 下架
     * @param id 主键 ID
     * @return 是否成功
     */
    Boolean unpublish(Long id);
}
```

### 2. Service 实现类

请生成 Service 实现类：

```java
package org.springblade.{模块目录}.service.impl;

import com.baomidou.mybatisplus.core.metadata.IPage;
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import org.springblade.{模块目录}.mapper.{模块类名}Mapper;
import org.springblade.{模块目录}.entity.{模块类名};
import org.springblade.{模块目录}.vo.{模块类名}VO;
import org.springblade.{模块目录}.dto.{模块类名}SaveVO;
import org.springblade.{模块目录}.dto.{模块类名}UpdateVO;
import org.springblade.{模块目录}.service.I{模块类名}Service;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import org.springframework.beans.BeanUtils;
import org.springblade.core.tool.utils.Func;
import org.springblad e.core.tool.api.R;
import org.springblade.core.tool.utils.StringPool;
import org.springblade.core.tool.utils.StringUtil;
import java.util.*;
import java.util.stream.Collectors;

/**
 * {模块名称} Service 实现类
 *
 * @author {作者}
 * @since {日期}
 */
@Service
public class {模块类名}ServiceImpl extends ServiceImpl<{模块类名}Mapper, {模块类名}> implements I{模块类名}Service {

    @Autowired
    private {子表 Mapper} {子表变量名}Mapper;

    @Override
    public IPage<{模块类名}VO> pageList(IPage page, Map<String, Object> params) {
        return baseMapper.select{模块类名}Page(page, params);
    }

    @Override
    public {模块类名}VO getDetail(Long id) {
        return baseMapper.selectById(id);
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public Boolean save{模块类名}({模块类名}SaveVO vo) {
        // 1. 生成实施编码（如需要）
        // String implementCode = generateImplementCode();

        // 2. 保存主表数据
        {模块类名} affair = new {模块类名}();
        BeanUtils.copyProperties(vo, affair);
        // affair.setImplementCode(implementCode);
        affair.setStatus(2); // 初始状态为下架
        baseMapper.insert(affair);

        // 3. 保存子表数据（如有）
        if (vo.getMaterials() != null && !vo.getMaterials().isEmpty()) {
            saveMaterials(affair.getId(), vo.getMaterials());
        }

        return true;
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public Boolean update{模块类名}({模块类名}UpdateVO vo) {
        // 1. 校验权限（仅创建人可修改）
        {模块类名} original = getById(vo.getId());
        if (!original.getCreateUser().equals(getCurrentUserId())) {
            throw new BusinessException("无权限修改他人数据");
        }

        // 2. 更新主表数据
        {模块类名} affair = new {模块类名}();
        BeanUtils.copyProperties(vo, affair);
        updateById(affair);

        // 3. 更新子表数据（先删后增，如有）
        if (vo.getMaterials() != null) {
            removeMaterialsByAffairId(vo.getId());
            if (!vo.getMaterials().isEmpty()) {
                saveMaterials(vo.getId(), vo.getMaterials());
            }
        }

        return true;
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public Boolean remove{模块类名}(String ids) {
        List<String> idList = Arrays.asList(ids.split(StringPool.COMMA));
        List<Long> longIds = idList.stream().map(Long::parseLong).collect(Collectors.toList());
        return removeByIds(longIds);
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public Boolean publish(Long id) {
        {模块类名} affair = getById(id);
        affair.setStatus(1); // 正常
        affair.setPublishTime(LocalDateTime.now());
        updateById(affair);
        return true;
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public Boolean unpublish(Long id) {
        {模块类名} affair = getById(id);
        affair.setStatus(2); // 下架
        updateById(affair);
        return true;
    }

    // 辅助方法（根据需要）
    private void saveMaterials(Long affairId, List<MaterialSaveVO> materials) {
        // 保存材料关联数据
    }

    private void removeMaterialsByAffairId(Long affairId) {
        // 删除材料关联数据
    }

    private Long getCurrentUserId() {
        // 获取当前用户 ID
        return Func.toLong(AuthUtil.getUserId());
    }
}
```

## 输出要求

1. Service 接口方法定义清晰
2. 实现类包含事务控制
3. 包含权限校验逻辑
4. 子表数据处理正确

## 补充说明

BladeX Service 规范：
- 继承 ServiceImpl 获得基础 CRUD 能力
- 使用@Transactional 控制事务
- 使用 BusinessException 抛出业务异常
- 使用 Func、StringPool 等 BladeX 工具类
```

---

## 提示词 3A-4：Controller 层开发

**使用场景**：Service 层完成后，完善 Controller 层

**复制以下内容给 Claude**：

```
Service 层已完成，现在需要完善 Controller 层。

## 项目信息

- 模块名称：{模块名称}
- 模块代码：{模块代码}
- 所属模块目录：{所属模块目录}
- 后端项目路径：{后端项目路径}

## 任务要求

### 1. Controller 完整实现

请生成完整的 Controller 类：

```java
package org.springblade.{模块目录}.controller;

import com.baomidou.mybatisplus.core.metadata.IPage;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.Parameter;
import io.swagger.v3.oas.annotations.tags.Tag;
import io.swagger.v3.oas.annotations.responses.ApiResponse;
import lombok.AllArgsConstructor;
import org.springblade.core.tool.api.R;
import org.springblade.core.tool.utils.Func;
import org.springblade.core.log.annotation.Log;
import org.springblade.core.security.auth.AuthUtil;
import org.springblade.core.mp.support.Query;
import org.springblade.core.mp.support.QueryParameterMap;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;
import org.springblade.{模块目录}.service.I{模块类名}Service;
import org.springblade.{模块目录}.vo.{模块类名}VO;
import org.springblade.{模块目录}.dto.{模块类名}SaveVO;
import org.springblade.{模块目录}.dto.{模块类名}UpdateVO;
import java.util.Map;

import javax.validation.Valid;
import javax.validation.constraints.NotBlank;

/**
 * {模块名称} Controller
 *
 * @author {作者}
 * @since {日期}
 */
@RestController
@AllArgsConstructor
@Validated
@RequestMapping(AppConstant.APPLICATION_{模块目录}_NAME + "/{模块代码}")
@Tag(name = "{模块名称}", description = "{模块名称}接口")
public class {模块类名}Controller {

    private final I{模块类名}Service {模块变量名}Service;

    /**
     * 分页查询
     */
    @GetMapping("/list")
    @Operation(summary = "分页查询", description = "传入分页参数和查询条件")
    public R<IPage<{模块类名}VO>> list(
        @Parameter(hidden = true) @QueryParameterMap Long tenantId,
        @RequestParam(defaultValue = "1") int current,
        @RequestParam(defaultValue = "10") int size,
        @RequestParam(required = false) String affairName,
        @RequestParam(required = false) String affairType,
        @RequestParam(required = false) Integer status
    ) {
        Query<{模块类名}VO> query = new Query<>(current, size);
        Map<String, Object> params = Func.toMap(
            "affairName", affairName,
            "affairType", affairType,
            "status", status
        );
        IPage<{模块类名}VO> pages = {模块变量名}Service.pageList(query, params);
        return R.data(pages);
    }

    /**
     * 详情查询
     */
    @GetMapping("/detail")
    @Operation(summary = "详情查询", description = "传入主键 ID")
    public R<{模块类名}VO> detail(@RequestParam Long id) {
        {模块类名}VO vo = {模块变量名}Service.getDetail(id);
        return R.data(vo);
    }

    /**
     * 新增
     */
    @PostMapping("/save")
    @Log("新增{模块名称}")
    @Operation(summary = "新增", description = "传入{模块名称}对象")
    public R<Boolean> save(@Valid @RequestBody {模块类名}SaveVO vo) {
        Boolean result = {模块变量名}Service.save{模块类名}(vo);
        return R.status(result);
    }

    /**
     * 修改
     */
    @PostMapping("/update")
    @Log("修改{模块名称}")
    @Operation(summary = "修改", description = "传入{模块名称}对象")
    public R<Boolean> update(@Valid @RequestBody {模块类名}UpdateVO vo) {
        Boolean result = {模块变量名}Service.update{模块类名}(vo);
        return R.status(result);
    }

    /**
     * 删除
     */
    @PostMapping("/remove")
    @Log("删除{模块名称}")
    @Operation(summary = "删除", description = "传入主键 ID 列表")
    public R<Boolean> remove(@RequestBody @NotBlank(message = "ID 不能为空") String ids) {
        Boolean result = {模块变量名}Service.remove{模块类名}(ids);
        return R.status(result);
    }

    /**
     * 发布
     */
    @PostMapping("/publish")
    @Log("发布{模块名称}")
    @Operation(summary = "发布", description = "传入主键 ID")
    public R<Boolean> publish(@RequestBody @NotBlank(message = "ID 不能为空") Long id) {
        Boolean result = {模块变量名}Service.publish(id);
        return R.status(result);
    }

    /**
     * 下架
     */
    @PostMapping("/unpublish")
    @Log("下架{模块名称}")
    @Operation(summary = "下架", description = "传入主键 ID")
    public R<Boolean> unpublish(@RequestBody @NotBlank(message = "ID 不能为空") Long id) {
        Boolean result = {模块变量名}Service.unpublish(id);
        return R.status(result);
    }
}
```

### 2. 权限注解配置

请在 Controller 类上添加权限注解：

```java
@PreAuth(menu = "{模块代码}")
```

### 3. Swagger 文档配置

确保 Swagger 文档注释完整：
- @Tag 描述类
- @Operation 描述方法
- @Parameter 描述参数
- @ApiResponse 描述响应

## 输出要求

1. Controller 代码符合 BladeX 规范
2. 权限注解正确配置
3. 日志注解正确配置
4. 参数校验完整
5. Swagger 文档注释完整

## 补充说明

BladeX Controller 规范：
- 使用@RestController 声明 REST 控制器
- 使用@RequestMapping 声明基础路径
- 使用@PreAuth 控制权限
- 使用@Log 记录操作日志
- 使用@Validated 开启参数校验
- 统一返回 R<T>包装类
```

---

## 执行顺序建议

1. 先执行 **提示词 3A-1**，完成 Entity 和 VO 类
2. 执行 **提示词 3A-2**，完成 Mapper 层
3. 执行 **提示词 3A-3**，完成 Service 层
4. 执行 **提示词 3A-4**，完成 Controller 层
5. 编写单元测试（可选）
6. 使用 Swagger 测试接口

---

## 阶段 3A 输出清单

完成后端开发后，你应该拥有：

- [ ] Entity 实体类
- [ ] VO 视图对象
- [ ] DTO 保存/修改对象
- [ ] Mapper 接口和 XML
- [ ] Service 接口和实现类
- [ ] Controller 类
- [ ] Swagger 接口文档
- [ ] 单元测试（可选）

---

## 下一步

后端开发完成后：
1. 启动后端服务
2. 使用 Swagger 测试接口
3. 与前端联调，使用 `04-联调测试提示词.md`
