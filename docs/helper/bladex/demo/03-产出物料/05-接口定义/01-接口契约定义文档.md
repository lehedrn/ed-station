# 阶段 2：接口定义 - 政务服务事项管理系统

> 接口契约定义文档 - 前后端并行开发的基础

---

## 文档信息

| 项目名称 | 政务服务事项管理系统 |
|---------|---------------------|
| 文档版本 | V1.0 |
| 创建日期 | 2026-04-02 |
| 文档类型 | 接口契约定义文档 |
| 状态 | 待确认 |

## 确认签字

| 角色 | 姓名 | 日期 |
|------|------|------|
| 后端开发 | ________ | ________ |
| 前端开发 | ________ | ________ |

---

## 1. 接口列表汇总表

| 序号 | 接口名称 | URL | 方法 | 权限码 | 说明 |
|------|----------|-----|------|--------|------|
| 1 | 分页查询 | `/blade-affair/affair/list` | GET | affair_manage_view | 分页查询事项列表 |
| 2 | 详情查询 | `/blade-affair/affair/detail` | GET | affair_manage_view | 查询详情（含材料子表） |
| 3 | 事项新增 | `/blade-affair/affair/save` | POST | affair_manage_add | 新增事项（含材料） |
| 4 | 事项修改 | `/blade-affair/affair/update` | POST | affair_manage_edit | 修改事项（含材料） |
| 5 | 事项删除 | `/blade-affair/affair/remove` | POST | affair_manage_delete | 删除事项（支持批量） |
| 6 | 事项发布 | `/blade-affair/affair/publish` | POST | affair_manage_publish | 发布事项 |
| 7 | 事项下架 | `/blade-affair/affair/unpublish` | POST | affair_manage_unpublish | 下架事项 |

---

## 2. 接口详细定义

### 2.1 分页查询接口

**接口描述**: 分页查询事项列表，支持条件筛选

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| current | int | 是 | 当前页码（从 1 开始） | 1 |
| size | int | 是 | 每页条数 | 10 |
| affairName | string | 否 | 事项名称（模糊查询） | 测试 |
| affairType | string | 否 | 事项类别（精确匹配） | 01 |
| status | int | 否 | 事项状态（1-正常 2-下架） | 1 |

**响应结构**:
```json
{
  "code": 200,
  "success": true,
  "msg": "查询成功",
  "data": {
    "records": [
      {
        "id": "165298986951905381",
        "affairName": "测试事项 1",
        "affairShortName": "测试 1",
        "implementCode": "AFFAIR20260402103000000001",
        "affairType": "01",
        "affairTypeDict": "行政许可",
        "legalLimit": 20,
        "promiseLimit": 10,
        "status": 1,
        "statusDict": "正常",
        "publishTime": "2026-04-02 14:00:00",
        "createTime": "2026-04-02 10:30:00"
      }
    ],
    "total": 100,
    "size": 10,
    "current": 1,
    "pages": 10
  }
}
```

---

### 2.2 详情查询接口

**接口描述**: 查询事项详情，包含材料子表信息

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | string | 是 | 事项 ID |

**响应结构**:
```json
{
  "code": 200,
  "success": true,
  "msg": "查询成功",
  "data": {
    "id": "165298986951905381",
    "affairName": "测试事项 1",
    "affairShortName": "测试 1",
    "implementCode": "AFFAIR20260402103000000001",
    "affairType": "01",
    "affairTypeDict": "行政许可",
    "legalLimit": 20,
    "promiseLimit": 10,
    "handleCondition": "<p>办理条件内容</p>",
    "remark": "备注说明",
    "publishTime": "2026-04-02 14:00:00",
    "status": 1,
    "statusDict": "正常",
    "materials": [
      {
        "id": "165298986951905391",
        "materialName": "材料 1",
        "materialType": "01",
        "materialTypeDict": "原件",
        "materialCopies": 1,
        "materialRemark": "材料说明",
        "attachId": "1111111111111111111",
        "attach": {
          "id": "1111111111111111111",
          "fileName": "test.pdf",
          "filePath": "/attach/2026/04/02/test.pdf",
          "originalName": "原始文件.pdf",
          "fileSize": 102400,
          "fileType": "application/pdf"
        }
      }
    ]
  }
}
```

---

### 2.3 事项新增接口

**接口描述**: 新增事项，可包含材料列表

**请求体**:
```json
{
  "affairName": "测试事项",
  "affairShortName": "测试",
  "affairType": "01",
  "legalLimit": 20,
  "promiseLimit": 10,
  "handleCondition": "<p>办理条件内容</p>",
  "remark": "备注说明",
  "materials": [
    {
      "materialName": "材料 1",
      "materialType": "01",
      "materialCopies": 1,
      "materialRemark": "材料说明",
      "attachId": "1111111111111111111"
    }
  ]
}
```

**响应结构**:
```json
{
  "code": 200,
  "success": true,
  "msg": "保存成功",
  "data": true
}
```

---

### 2.4 事项修改接口

**接口描述**: 修改事项，可同步更新材料列表

**请求体**:
```json
{
  "id": "165298986951905381",
  "affairName": "修改后的事项名称",
  "affairShortName": "修改",
  "affairType": "01",
  "legalLimit": 20,
  "promiseLimit": 10,
  "handleCondition": "<p>修改后的办理条件</p>",
  "remark": "修改备注",
  "materials": [
    {
      "id": "165298986951905391",
      "materialName": "修改材料",
      "materialType": "01",
      "materialCopies": 2,
      "materialRemark": "修改说明",
      "attachId": "1111111111111111111"
    }
  ]
}
```

**响应结构**: 同新增接口

---

### 2.5 事项删除接口

**接口描述**: 删除事项（逻辑删除），支持批量删除

**请求体**:
```json
{
  "ids": "165298986951905381,165298986951905382"
}
```

**响应结构**:
```json
{
  "code": 200,
  "success": true,
  "msg": "删除成功",
  "data": true
}
```

---

### 2.6 事项发布接口

**接口描述**: 发布事项，状态变更为正常

**请求体**:
```json
{
  "id": "165298986951905381"
}
```

**响应结构**:
```json
{
  "code": 200,
  "success": true,
  "msg": "发布成功",
  "data": true
}
```

---

### 2.7 事项下架接口

**接口描述**: 下架事项，状态变更为下架

**请求体**:
```json
{
  "id": "165298986951905381"
}
```

**响应结构**: 同发布接口

---

## 3. 字段命名映射说明

| 数据库字段（下划线） | 前端字段（驼峰） | 说明 |
|---------------------|-----------------|------|
| id | id | 主键 ID |
| affair_name | affairName | 事项名称 |
| affair_short_name | affairShortName | 事项简称 |
| implement_code | implementCode | 实施编码 |
| affair_type | affairType | 事项类别字典值 |
| legal_limit | legalLimit | 法定时限 |
| promise_limit | promiseLimit | 承诺时限 |
| handle_condition | handleCondition | 办理条件 |
| publish_time | publishTime | 发布时间 |
| status | status | 事项状态 |
| create_user | createUser | 创建人 ID |
| create_time | createTime | 创建时间 |
| update_user | updateUser | 更新人 ID |
| update_time | updateTime | 更新时间 |
| is_deleted | isDeleted | 逻辑删除标识 |

---

## 4. 字典字段处理

| 数据库字段 | 前端字段 | 字典值字段 | 字典接口 |
|-----------|---------|-----------|---------|
| affair_type | affairType | affairTypeDict | `/blade-system/dict/dictionary?code=affair_type` |
| status | status | statusDict | `/blade-system/dict/dictionary?code=affair_status` |
| material_type | materialType | materialTypeDict | `/blade-system/dict/dictionary?code=material_type` |

---

## 5. 后端 Controller 框架代码

```java
package org.springblade.affair.controller;

import com.github.xiaoymin.knife4j.annotations.ApiOperationSupport;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import io.swagger.annotations.ApiParam;
import lombok.AllArgsConstructor;
import org.springblade.affair.service.IAffairService;
import org.springblade.affair.vo.AffairDetailVO;
import org.springblade.affair.vo.AffairSaveVO;
import org.springblade.affair.vo.AffairUpdateVO;
import org.springblade.affair.vo.AffairVO;
import org.springblade.common.constant.AppConstant;
import org.springblade.core.mp.support.Condition;
import org.springblade.core.mp.support.Query;
import org.springblade.core.tool.api.R;
import org.springblade.core.tool.utils.Func;
import org.springblade.core.tool.utils.StringPool;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.bind.annotation.RequestParam;

import javax.validation.Valid;
import java.util.Map;

import com.baomidou.mybatisplus.core.metadata.IPage;
import org.springblade.core.tool.jackson.JsonUtil;
import org.springblade.core.tool.jackson.type.TypeRef;

import io.swagger.v3.oas.annotations.tags.Tag;
import io.swagger.v3.oas.annotations.Parameter;
import org.springblade.core.boot.ctrl.BladeController;
import org.springblade.core.tool.jackson.type.TypeRef;
import org.springblade.core.tool.utils.StringPool;
import org.springblade.core.tool.api.R;

import java.util.HashMap;
import java.util.Map;
import org.springblade.core.tool.jackson.JsonUtil;
import org.springframework.web.bind.annotation.RestController;
import org.springblade.core.tool.utils.Func;
import org.springblade.core.mp.support.Query;
import org.springblade.core.mp.support.Condition;
import org.springblade.core.tool.api.R;
import org.springblade.affair.service.IAffairService;
import org.springblade.affair.vo.*;
import io.swagger.v3.oas.annotations.tags.Tag;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.Parameter;
import org.springblade.core.boot.ctrl.BladeController;
import lombok.AllArgsConstructor;
import org.springframework.web.bind.annotation.*;
import javax.validation.Valid;
import org.springblade.core.tool.utils.StringPool;

/**
 * 事项管理 Controller
 *
 * @author coderleehd
 * @since 2026-04-02
 */
@RestController
@AllArgsConstructor
@RequestMapping(AppConstant.APPLICATION_AFFAIR_NAME + "/affair")
@Tag(name = "事项管理", description = "事项管理接口")
public class AffairController extends BladeController<AffairVO, AffairService, Long> {

    private final IAffairService affairService;

    /**
     * 分页查询
     */
    @GetMapping("/list")
    @ApiOperationSupport(order = 2)
    @Operation(summary = "分页查询", description = "传入分页参数和查询条件")
    public R<IPage<AffairVO>> list(
        @ApiParam(hidden = true) @ParameterMap Long tenantId,
        @RequestParam(defaultValue = "1") int current,
        @RequestParam(defaultValue = "10") int size,
        @RequestParam(required = false) String affairName,
        @RequestParam(required = false) String affairType,
        @RequestParam(required = false) Integer status
    ) {
        Query query = new Query(current, size, Func.toJsonNull(new HashMap<>()));
        Map<String, Object> params = query.cond();
        params.put("affairName", affairName);
        params.put("affairType", affairType);
        params.put("status", status);
        return R.data(affairService.pageList(page(query), params));
    }

    /**
     * 详情查询
     */
    @GetMapping("/detail")
    @ApiOperationSupport(order = 1)
    @Operation(summary = "详情", description = "传入事项 ID")
    public R<AffairDetailVO> detail(@RequestParam Long id) {
        AffairVO vo = affairService.getById(id);
        return R.data(affairService.getDetail(id));
    }

    /**
     * 事项新增
     */
    @PostMapping("/save")
    @ApiOperationSupport(order = 3)
    @Operation(summary = "新增", description = "传入事项信息")
    public R<Boolean> save(@Valid @RequestBody AffairSaveVO vo) {
        return R.data(affairService.saveAffair(vo));
    }

    /**
     * 事项修改
     */
    @PostMapping("/update")
    @ApiOperationSupport(order = 4)
    @Operation(summary = "修改", description = "传入事项信息")
    public R<Boolean> update(@Valid @RequestBody AffairUpdateVO vo) {
        return R.data(affairService.updateAffair(vo));
    }

    /**
     * 事项删除
     */
    @PostMapping("/remove")
    @ApiOperationSupport(order = 5)
    @Operation(summary = "删除", description = "传入事项 ID（支持批量）")
    public R<Boolean> remove(@ApiParam(value = "事项 ID(逗号分隔)", required = true) 
                             @RequestParam String ids) {
        return R.data(affairService.removeAffair(ids));
    }

    /**
     * 事项发布
     */
    @PostMapping("/publish")
    @ApiOperationSupport(order = 6)
    @Operation(summary = "发布", description = "传入事项 ID")
    public R<Boolean> publish(@RequestParam Long id) {
        return R.data(affairService.publish(id));
    }

    /**
     * 事项下架
     */
    @PostMapping("/unpublish")
    @ApiOperationSupport(order = 7)
    @Operation(summary = "下架", description = "传入事项 ID")
    public R<Boolean> unpublish(@RequestParam Long id) {
        return R.data(affairService.unpublish(id));
    }
}
```

---

## 6. 前端 API 封装框架代码

```javascript
/**
 * 事项管理 API 接口封装
 * @module api/affair/manage
 */
import request from '@/axios';

/**
 * 分页查询事项列表
 * @param {number} current - 当前页码
 * @param {number} size - 每页条数
 * @param {object} params - 查询条件
 * @returns {Promise}
 */
export const getList = (current, size, params) => {
  return request({
    url: '/blade-affair/affair/list',
    method: 'get',
    params: { ...params, current, size },
    cryptoToken: false,
    cryptoData: false,
  });
};

/**
 * 详情查询
 * @param {string|number} id - 事项 ID
 * @returns {Promise}
 */
export const getDetail = (id) => {
  return request({
    url: '/blade-affair/affair/detail',
    method: 'get',
    params: { id },
    cryptoToken: false,
    cryptoData: false,
  });
};

/**
 * 事项新增
 * @param {object} data - 事项数据
 * @returns {Promise}
 */
export const save = (data) => {
  return request({
    url: '/blade-affair/affair/save',
    method: 'post',
    data,
    cryptoToken: false,
    cryptoData: false,
  });
};

/**
 * 事项修改
 * @param {object} data - 事项数据（包含 id）
 * @returns {Promise}
 */
export const update = (data) => {
  return request({
    url: '/blade-affair/affair/update',
    method: 'post',
    data,
    cryptoToken: false,
    cryptoData: false,
  });
};

/**
 * 事项删除（支持批量）
 * @param {string} ids - 事项 ID（逗号分隔）
 * @returns {Promise}
 */
export const remove = (ids) => {
  return request({
    url: '/blade-affair/affair/remove',
    method: 'post',
    data: { ids },
    cryptoToken: false,
    cryptoData: false,
  });
};

/**
 * 事项发布
 * @param {string|number} id - 事项 ID
 * @returns {Promise}
 */
export const publish = (id) => {
  return request({
    url: '/blade-affair/affair/publish',
    method: 'post',
    data: { id },
    cryptoToken: false,
    cryptoData: false,
  });
};

/**
 * 事项下架
 * @param {string|number} id - 事项 ID
 * @returns {Promise}
 */
export const unpublish = (id) => {
  return request({
    url: '/blade-affair/affair/unpublish',
    method: 'post',
    data: { id },
    cryptoToken: false,
    cryptoData: false,
  });
};

/**
 * 文件上传
 * @param {File} file - 文件对象
 * @returns {Promise}
 */
export const uploadFile = (file) => {
  const formData = new FormData();
  formData.append('file', file);
  return request({
    url: '/blade-resource/attach/upload',
    method: 'post',
    data: formData,
    headers: {
      'Content-Type': 'multipart/form-data',
    },
    cryptoToken: false,
    cryptoData: false,
  });
};
```

---

## 7. Avue Column 配置建议

```javascript
/**
 * 事项管理 Avue 配置
 * @module views/affair/manage/index
 */
export const option = {
  title: '事项信息管理',
  formWidth: '1000',
  form: true,
  formTips: '带*号字段为必填项',
  menu: true,
  menuType: 'fixed',
  addBtn: true,
  editBtn: true,
  delBtn: true,
  viewBtn: true,
  searchBtn: true,
  dateBtn: true,
  refreshBtn: true,
  selection: true,
  border: true,
  stripe: true,
  column: [
    {
      label: '事项名称',
      prop: 'affairName',
      type: 'text',
      search: true,
      span: 24,
      rules: [{ required: true, message: '请输入事项名称', trigger: 'blur' }],
      placeholder: '请输入事项名称，最多 200 字'
    },
    {
      label: '事项简称',
      prop: 'affairShortName',
      type: 'text',
      span: 24,
      placeholder: '请输入事项简称，最多 100 字'
    },
    {
      label: '事项类别',
      prop: 'affairType',
      type: 'select',
      search: true,
      span: 12,
      dicUrl: '/blade-system/dict/dictionary?code=affair_type',
      props: { label: 'dictValue', value: 'dictKey' },
      dataType: 'string',
      rules: [{ required: true, message: '请选择事项类别', trigger: 'change' }]
    },
    {
      label: '法定时限',
      prop: 'legalLimit',
      type: 'number',
      span: 12,
      min: 0,
      placeholder: '请输入法定时限（工作日）',
      rules: [{ required: true, message: '请输入法定时限', trigger: 'blur' }]
    },
    {
      label: '承诺时限',
      prop: 'promiseLimit',
      type: 'number',
      span: 12,
      min: 0,
      placeholder: '请输入承诺时限（工作日）',
      rules: [
        { required: true, message: '请输入承诺时限', trigger: 'blur' },
        { 
          validator: (rule, value, callback, source, options) => {
            const legalLimit = options.formData.legalLimit;
            if (value > legalLimit) {
              callback(new Error('承诺时限不能大于法定时限'));
            } else {
              callback();
            }
          }, 
          trigger: 'blur' 
        }
      ]
    },
    {
      label: '办理条件',
      prop: 'handleCondition',
      type: 'ueditor',
      span: 24,
      minRows: 10,
      rules: [{ required: true, message: '请输入办理条件', trigger: 'blur' }]
    },
    {
      label: '备注说明',
      prop: 'remark',
      type: 'textarea',
      span: 24,
      minRows: 3,
      maxlength: 500,
      showWordLimit: true
    },
    {
      label: '事项状态',
      prop: 'status',
      type: 'select',
      search: true,
      span: 12,
      dicUrl: '/blade-system/dict/dictionary?code=affair_status',
      props: { label: 'dictValue', value: 'dictKey' },
      dataType: 'number',
      slot: true
    },
    {
      label: '发布时间',
      prop: 'publishTime',
      type: 'datetime',
      span: 12,
      format: 'YYYY-MM-DD HH:mm:ss',
      valueFormat: 'YYYY-MM-DD HH:mm:ss',
      readonly: true,
      editDisplay: true,
      addDisplay: false
    },
    {
      label: '创建时间',
      prop: 'createTime',
      type: 'datetime',
      span: 12,
      format: 'YYYY-MM-DD HH:mm:ss',
      valueFormat: 'YYYY-MM-DD HH:mm:ss',
      readonly: true,
      editDisplay: true,
      addDisplay: false
    }
  ],
  beforeAdd: () => {
    // 新增前处理
    return true;
  },
  beforeUpdate: (done, formData) => {
    // 修改前处理
    done();
  }
};
```

---

## 8. VO 对象定义

### 8.1 后端 VO 定义

```java
/**
 * 事项保存 VO
 */
@Data
@ApiModel(value = "事项保存对象", description = "事项新增/修改请求对象")
public class AffairSaveVO implements Serializable {
    
    private static final long serialVersionUID = 1L;
    
    @ApiModelProperty(value = "事项名称", required = true)
    @NotBlank(message = "事项名称不能为空")
    private String affairName;
    
    @ApiModelProperty(value = "事项简称")
    private String affairShortName;
    
    @ApiModelProperty(value = "事项类别", required = true)
    @NotBlank(message = "事项类别不能为空")
    private String affairType;
    
    @ApiModelProperty(value = "法定时限", required = true)
    @NotNull(message = "法定时限不能为空")
    private Integer legalLimit;
    
    @ApiModelProperty(value = "承诺时限", required = true)
    @NotNull(message = "承诺时限不能为空")
    private Integer promiseLimit;
    
    @ApiModelProperty(value = "办理条件", required = true)
    @NotBlank(message = "办理条件不能为空")
    private String handleCondition;
    
    @ApiModelProperty(value = "备注说明")
    private String remark;
    
    @ApiModelProperty(value = "材料列表")
    private List<MaterialSaveVO> materials;
}

/**
 * 材料保存 VO
 */
@Data
@ApiModel(value = "材料保存对象", description = "材料新增/修改请求对象")
public class MaterialSaveVO implements Serializable {
    
    private static final long serialVersionUID = 1L;
    
    @ApiModelProperty(value = "材料名称", required = true)
    @NotBlank(message = "材料名称不能为空")
    private String materialName;
    
    @ApiModelProperty(value = "材料类型", required = true)
    @NotBlank(message = "材料类型不能为空")
    private String materialType;
    
    @ApiModelProperty(value = "份数要求", required = true)
    @NotNull(message = "份数要求不能为空")
    private Integer materialCopies;
    
    @ApiModelProperty(value = "材料说明")
    private String materialRemark;
    
    @ApiModelProperty(value = "附件 ID", required = true)
    @NotNull(message = "附件 ID 不能为空")
    private Long attachId;
}
```

### 8.2 前端 VO 定义

```javascript
/**
 * 事项 VO 默认值
 */
export const AffairDefaultVO = {
  id: null,
  affairName: '',
  affairShortName: '',
  implementCode: '',
  affairType: '',
  legalLimit: 0,
  promiseLimit: 0,
  handleCondition: '',
  remark: '',
  publishTime: null,
  status: 2, // 默认下架状态
  createTime: null,
  updateTime: null
};

/**
 * 材料 VO 默认值
 */
export const MaterialDefaultVO = {
  id: null,
  affairId: null,
  attachId: null,
  materialName: '',
  materialType: '',
  materialCopies: 1,
  materialRemark: '',
  sort: 0,
  attach: null
};
```

---

## 9. 附录

### 9.1 BladeX 规范说明

1. **基础路径**: `/blade-affair/affair`
2. **统一响应格式**: `R<T>`，包含 code、success、msg、data
3. **分页响应**: 包含 records、total、size、current、pages
4. **权限注解**: `@PreAuth(menu = "权限码")`

### 9.2 字符集说明

- 数据库字符集：`utf8mb4`
- 连接字符集：`utf8mb4`
- 所有包含中文的 SQL 脚本执行时需指定 `--default-character-set=utf8mb4`

---

## 10. 下一步

完成接口定义后，进入**阶段 3：并行开发**：

- **后端开发**: 使用 `03A-后端开发提示词.md`
- **前端开发**: 使用 `03B-前端开发提示词.md`
